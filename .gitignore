bin/
obj/
.vs/
*.user
*.suo

# Never commit the real secret. Ship appsettings.example.json instead.
appsettings.json
appsettings.*.json
!appsettings.example.json

# Saved multi-tenant connections (contain client secrets).
tenants.json

# Local metric history for trend deltas.
snapshots.json
